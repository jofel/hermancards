<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Név" %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label "Szoba" %>
    <%= f.text_field :room %>
  </div>

  <div class="field">
    <%= f.label "Kép" %>
    <%= f.text_field :pic, :value => "pic_0000.png" %>
    <!-- <%= form_for @user do |f| %>
      <%= f.file_field :pic %>
    <% end %> -->
  </div>

  <div class="actions">
    <%= f.submit do %>
      <i class="fa fa-check-circle" aria-hidden="true"></i>
    <% end %>  
  </div>
<% end %>
